# Base URL of the AutoFlow Research backend (FastAPI).
# The frontend reads this at build/runtime via process.env.NEXT_PUBLIC_API_BASE.
# Defaults to http://localhost:8000 when unset.
NEXT_PUBLIC_API_BASE=http://localhost:8000
